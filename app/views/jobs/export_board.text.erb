#### <%= board['project'] %>

# <%= board['title'] %>

### <%= board['description'] %>

---

## Cards

<% board['cards'].each do |card| %>
##### <%= card['question'] %>

#### <%= card['title'] %>

<%= card['content'] %>

 * <%= card['type'] %> by <%= card['author'] %>
<% card['data'].each do |attr_key, attr_val| %><% if attr_key == 'items' %><% attr_val.each do |item| %>  * <%= item['checked'] ? '☑' : '☐' %> <%= item['label'] %>
<% end %><% else %> * <%= attr_key.humanize %>: <%= attr_val %><% end %>
<% end %>
---

<% end %>
