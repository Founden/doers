= _('Hi %s,') % @user.nicename
\
\
= _('%s participated in %s discussions:') % [@usernames, @project.title]
\
- @activities.each do |act|
  = precede ' * ' do
    = render(:partial => act.slug.underscore, :locals => {:activity => act})
\
\
= _('Manage notifications:')
= profile_notifications_url(:mine)
\
= _('Thanks.')
= _('Your %s team.') % Doers::Config.app_name
