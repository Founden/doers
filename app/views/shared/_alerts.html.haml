#notifications
  .row
    .large-4.large-offset-4.columns
      - flash.each do |name, msg|
        - if msg.is_a?(String)
          .align-center{ :class => "alert-box #{name} radius", 'data-alert' => true }
            = msg
            %a.close{ :href => '#' }
              &times;
