#profile

  %header.header
    .header-content
      = gravatar_tag(@profile.email, {:class => 'avatar', :size => 58})
      %h1
        = @profile.nicename
      %p
        = mail_to @profile.email, @profile.email
