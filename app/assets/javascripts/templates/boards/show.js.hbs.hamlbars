#board

  %header.header
    %h1
      {{title}}
    .buttons
      %a.button
        = _('Add new card')

  .toolbar
    .tabs
      %ul
        - %w(Media Text).each do |title|
          %li
            %a{:href => ''}
              = title
    {{view view.cardTypesView}}

  %div{:bind => {:id => 'slug'}}
    {{#if cards.isLoaded}}
    {{view view.cardsView contentBinding=cards}}
    {{/if}}
