#boards

  %header.header
    .header-content
      %h1
        = _('Boards built by you')
    .buttons
      {{#linkTo 'boards.new' class='button'}}
      = _('Build a new board')
      {{/linkTo}}

  .content
    .boards
      {{#each content}}
      .board{:bind => {:id => 'slug'}}
        {{#linkTo 'boards.build' this}}
        %h2 {{title}}
        {{/linkTo}}
        %p.description {{description}}
        %ul.meta
          %li
            = _('Updated on')
            {{lastUpdate}}
          %li
            {{cardsCount}}
            = _('cards available')
        %footer
          = _('Branched')
          {{branchesCount}}
          {{#if branchesCount}}
          = _('times')
          {{else}}
          = _('time')
          {{/if}}

        {{partial 'partials/delete_confirmation'}}

      {{/each}}
