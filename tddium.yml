:tddium:
  :ruby_version: ruby-2.0.0-p247
  :bundler_version: 1.5.0.rc.1
  :hooks:
    :worker_setup: bundle config jobs `cat /proc/cpuinfo | grep processor | wc -l`
  :sqlite: false
  :postgresql:
    :version: '9.2'
    :config:
      pool: 5
