box: wercker/ubuntu12.04-ruby2.0.0

services:
  - wercker/postgresql

build:
  steps:
    - install-packages:
      packages: libv8-dev
    - bundle-install
    - script:
      name: RSpec
      script: bundle exec rspec
